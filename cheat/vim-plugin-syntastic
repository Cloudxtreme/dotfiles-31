# Syntastic

# to show the list window
`:Errors` <leader>le

# close the list window
`:lclose` <leader>lc

# go to the next error
`:lnext` <leader>ln

# go to the previous error
`:lprev` <leader>lp

# Toggle the active and passive mode
`:SyntasticToggleMode` <leader>lt

# conduct a manual check
`:SyntasticCheck` <leader>ll
